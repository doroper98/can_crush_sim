# GUIDE.md — Autoresearch 개발 체계 가이드

> 이 문서는 프로젝트 자체의 일부가 아니다.
> Karpathy autoresearch 패턴을 웹앱/소프트웨어 개발에 적용한 **마크다운 체계의 설명서**이다.
> 새 프로젝트를 시작할 때 이 문서를 읽고, 4개 파일을 복제하여 내용을 교체하면 된다.

---

## 1. 전체 개념

Andrej Karpathy의 autoresearch는 3개 파일로 AI 에이전트가 자율적으로 ML 연구를 수행하는 구조다. 이 체계는 그 원리를 범용 소프트웨어 개발에 확장 적용한 것이다.

```
인간은 "무엇을"과 "어떻게 판단할지"를 쓴다.
에이전트는 "어떻게 만들지"를 실행한다.
결과는 자동으로 기록되고, 성공만 살아남는다.
```

### 핵심 원리

```
┌─────────────┐
│   GOAL.md   │  ← 인간이 정의한 목표 (불변)
└──────┬──────┘
       │ 참조
┌──────▼──────┐
│ program.md  │  ← 인간이 에이전트에게 주는 행동 지시
└──────┬──────┘
       │ 실행
┌──────▼──────┐
│   src/ 등   │  ← 에이전트가 수정하는 코드
└──────┬──────┘
       │ 기록
┌──────▼──────┐
│ DEVLOG.md   │  ← 에이전트가 자동 기록하는 개발 로그
│ results.tsv │  ← 수치 데이터 (로컬 전용)
└─────────────┘
```

---

## 2. 파일별 설명

### 2.1 GOAL.md — 목표 명세서

**한줄 정의**: 프로젝트가 "완성되었을 때 어떤 모습인지"를 정의하는 문서.

**Karpathy 대응**: `prepare.py` (고정 상수, 변경 금지)

**포함 내용**:

| 섹션 | 설명 |
|------|------|
| 프로젝트 정의 | 프로젝트명, 한줄 정의, 최종 산출물, 성공 기준 |
| 기술 스택 | 사용 기술 확정 목록 (프레임워크, 라이브러리, 빌드 도구) |
| 디자인 시스템 | 컬러, 타이포그래피, UI 스타일 규칙 |
| 레이아웃 구조 | 화면 배치, 영역 비율, 구성 요소 |
| 상세 명세 | 도메인별 기술 스펙 (물리엔진, API, 데이터 모델 등) |
| 기능 요구사항 (FR) | FR-01 ~ FR-NN 형식의 기능 목록 |
| 비기능 요구사항 (NF) | NF-01 ~ NF-NN 형식의 품질/제약 목록 |
| 개발 로드맵 | Phase 정의, 단계별 산출물 |
| CHANGELOG | 명세 변경 이력 (날짜, 내용, 사유) |

**규칙**:
- 에이전트는 절대 수정하지 않는다
- 인간만 수정하며, 변경 시 CHANGELOG에 기록한다
- 모든 개발 판단의 최종 근거가 되는 "진실의 원천(source of truth)"

**새 프로젝트 적용 시**: SRS(소프트웨어 요구사항 명세서)의 핵심 내용을 이 형식에 맞춰 작성한다. FR/NF ID를 부여하는 것이 핵심이다.

---

### 2.2 program.md — 에이전트 행동 지시서

**한줄 정의**: 에이전트에게 "어떤 순서로, 어떤 기준으로, 어떻게 개발할지"를 가르치는 문서.

**Karpathy 대응**: `program.md` (인간이 편집하는 유일한 파일)

**포함 내용**:

| 섹션 | 설명 |
|------|------|
| 에이전트 역할 정의 | "너는 누구이고 무엇을 하는가" |
| 핵심 원칙 | 한 번에 하나만, 측정으로 판단, 실패는 되돌림 등 |
| 파일 구조와 수정 규칙 | 누가 어떤 파일을 수정하는지 명시 |
| 셋업 단계 | 최초 1회 실행할 초기화 절차 |
| 실험 루프 | 가설→수정→빌드→측정→판정→기록→push 순환 |
| 측정 메트릭 | Primary/Secondary 메트릭 정의, 목표값 |
| Phase별 상세 지시 | 각 Phase 내 Step 순서, 구현 방법, 판정 기준 |
| 자기 순환 개선 규칙 | 이전 실험에서 학습하는 방법 |
| 절대 규칙 (NEVER) | 에이전트가 절대 하면 안 되는 행위 목록 |
| Git 원격 저장소 운용 | push/pull 규칙, 멀티 환경 워크플로우 |
| 보고 규칙 | 커밋 메시지 형식, 동기화 상태 확인 |

**규칙**:
- 인간만 수정한다
- 에이전트의 "성격"과 "행동 양식"을 결정하는 핵심 파일
- 이 파일의 품질이 에이전트의 개발 품질을 결정한다

**새 프로젝트 적용 시**: 실험 루프의 "빌드 & 테스트" 명령어와 "측정 메트릭"을 프로젝트에 맞게 교체한다. Phase별 Step을 새 프로젝트의 구현 순서로 재작성한다. 나머지 구조(원칙, NEVER, Git 규칙)는 거의 그대로 재사용 가능하다.

---

### 2.3 DEVLOG.md — 개발 로그

**한줄 정의**: 에이전트가 자동으로 기록하는 시간순 개발 일지. 인간이 사후적으로 진행 상황을 파악·분석하는 용도.

**Karpathy 대응**: 원본 autoresearch에는 없는 확장 요소. results.tsv의 서술형 보완판.

**포함 내용**:

| 섹션 | 설명 |
|------|------|
| ID 체계 설명 | EXP, Step, FR, NF, BUG ID 형식 정의 |
| 추적 매트릭스 | Step ↔ FR/NF 매핑표. 각 Step의 상태(미착수/진행/완료) |
| 개발 로그 (본체) | 역시간순 실험 항목. 타임스탬프, ID, 변경, 테스트, 결과, 판정 |
| Phase 진행 현황 | Phase별 실험 수, 성공률, 시작/완료일 |
| 누적 통계 | 총 실험/커밋/리셋 수, 성공률, 현재 위치 |
| 버그 로그 | 발견된 결함 추적 (발견→해결) |
| 교훈 로그 | 실험에서 발견한 패턴, 노하우 축적 |

**각 로그 항목에 포함되는 정보**:

```
[EXP-NNN] 한줄 설명
├── 시각: YY/MM/DD HH:MM:SS
├── Step ID: S{Phase}.{Step}        ← program.md와 연결
├── 관련 FR: FR-NN                  ← GOAL.md와 연결
├── 관련 NF: NF-NN                  ← GOAL.md와 연결
├── 변경 내용: 무엇을 어떻게 바꿨는지
├── 테스트 항목: 무엇을 어떻게 테스트했는지
├── 테스트 결과: PASS / FAIL / PARTIAL
├── 측정값: build, fps, nodes, physics, ui score
├── 판정: COMMIT {hash} / RESET
└── 비고: 교훈, 다음 실험 힌트
```

**규칙**:
- 에이전트가 매 실험마다 자동 기록한다
- git에 커밋한다 (인간이 pull 받아 열람하기 위해)
- 인간은 읽기만 하고 수정하지 않는다

**새 프로젝트 적용 시**: 추적 매트릭스의 Step 목록을 새 program.md의 Step으로 교체한다. 로그 항목 형식은 거의 그대로 재사용 가능하다.

---

### 2.4 results.tsv — 수치 데이터 로그

**한줄 정의**: 실험 결과의 정량 데이터만 탭 구분으로 기록하는 경량 로그.

**Karpathy 대응**: `results.tsv` (동일)

**포함 컬럼**:

```
experiment_id | timestamp | phase | change_description | build_ok | render_fps | node_count | physics_ok | ui_score | result | notes
```

**규칙**:
- git에 커밋하지 않는다 (.gitignore에 포함)
- 로컬에서 데이터 분석용으로만 사용
- DEVLOG.md의 EXP ID와 1:1 대응

**새 프로젝트 적용 시**: 컬럼을 프로젝트의 측정 메트릭에 맞게 교체한다. 예를 들어 API 서버 프로젝트라면 `response_time_ms`, `error_rate`, `test_pass_count` 등.

---

### 2.5 README.md — 프로젝트 소개

**한줄 정의**: 이 프로젝트가 무엇이고, autoresearch 패턴을 어떻게 적용했는지 설명하는 문서.

**포함 내용**: 프로젝트 정의, 파일 구조, 작동 방식, 시작 방법, 인간의 역할.

**새 프로젝트 적용 시**: 프로젝트 설명과 기술 스택 부분만 교체.

---

### 2.6 .gitignore

**포함 항목**: `node_modules/`, `dist/`, `*.log`, `results.tsv` 등

**핵심**: `results.tsv`는 제외, `DEVLOG.md`는 포함. 이 구분이 중요하다.

---

## 3. 파일 간 관계도

```
GOAL.md (목표)
  │
  │ "무엇을 만들 것인가" (FR-01~15, NF-01~08)
  │
  ▼
program.md (지시)
  │
  │ "어떻게 만들 것인가" (S1.1~S4.8, 메트릭, 판정 기준)
  │
  ▼
src/ (코드) ──────────────→ 빌드 & 테스트
  │                              │
  │                              ▼
  │                        측정 (fps, build, physics...)
  │                              │
  │         ┌────────────────────┤
  │         ▼                    ▼
  │   results.tsv           DEVLOG.md
  │   (수치, 로컬)          (서술, 원격)
  │         │                    │
  │         └────────┬───────────┘
  │                  │
  │           ID로 교차 참조
  │         EXP-NNN ↔ S{P}.{S} ↔ FR-NN ↔ NF-NN
  │
  └──→ git commit (성공) 또는 git reset (실패)
              │
              ▼
        GitHub (원격)
         ↕ push/pull
       다른 환경 (회사 PC)
```

---

## 4. ID 체계 총정리

| ID 형식 | 소속 문서 | 용도 | 예시 |
|---------|-----------|------|------|
| **FR-{NN}** | GOAL.md | 기능 요구사항 | FR-01, FR-15 |
| **NF-{NN}** | GOAL.md | 비기능 요구사항 | NF-01, NF-08 |
| **S{P}.{S}** | program.md | 구현 Step | S1.1, S2.5, S4.8 |
| **EXP-{NNN}** | DEVLOG.md, results.tsv | 실험 번호 | EXP-001, EXP-127 |
| **BUG-{NNN}** | DEVLOG.md | 버그 번호 | BUG-001 |

**추적 경로 예시**:

```
"강체 위치 제어가 제대로 되나?" 라는 질문에 답하려면:

1. GOAL.md에서 FR-08 확인 → "강체 위치/방향 3D Gizmo 또는 수치입력 제어"
2. DEVLOG.md 추적 매트릭스에서 FR-08 → S2.5, S2.6 확인
3. DEVLOG.md 로그에서 S2.5, S2.6 관련 EXP 항목 검색
4. 해당 EXP의 테스트 결과, 판정, 비고 확인
5. 필요시 results.tsv에서 동일 EXP-NNN의 수치 데이터 확인
```

---

## 5. 새 프로젝트에 적용하는 방법

### Step 1: 4개 파일 복제

```
GOAL.md, program.md, DEVLOG.md, results.tsv
```

### Step 2: GOAL.md 재작성

- 프로젝트 정의, 기술 스택, 디자인 시스템을 새 프로젝트에 맞게 작성
- FR-01 ~ FR-NN, NF-01 ~ NF-NN 정의
- Phase별 로드맵 정의

### Step 3: program.md 수정

- 파일 구조를 새 프로젝트에 맞게 변경
- 셋업 명령어 변경 (npm, pip, cargo 등 프로젝트에 맞게)
- 빌드 & 테스트 명령어 변경
- 측정 메트릭을 프로젝트에 맞게 재정의
- Phase별 Step을 새로 작성 (GOAL.md의 FR/NF를 충족하는 순서)
- Git 원격 저장소 URL 변경

### Step 4: DEVLOG.md 초기화

- 추적 매트릭스를 새 program.md의 Step 목록으로 교체
- Step ↔ FR/NF 매핑 재작성
- 로그 영역 비우기
- 통계 초기화

### Step 5: results.tsv 초기화

- 헤더 컬럼을 새 측정 메트릭에 맞게 변경

### Step 6: 에이전트 실행

```
"GOAL.md와 program.md를 읽고 실험 루프를 시작해."
```

---

## 6. 프로젝트 유형별 메트릭 예시

이 체계는 어떤 프로젝트에든 적용 가능하다. 핵심은 **측정 메트릭**을 올바르게 정의하는 것이다.

| 프로젝트 유형 | Primary 메트릭 | Secondary 메트릭 |
|--------------|---------------|-----------------|
| **웹앱 (프론트엔드)** | build_ok, render_fps, lighthouse_score | a11y_score, bundle_size_kb |
| **API 서버** | build_ok, test_pass_count, response_time_ms | error_rate, memory_mb |
| **데이터 파이프라인** | build_ok, pipeline_ok, processing_time_s | data_quality_score, row_count |
| **ML 모델** | train_ok, val_loss, val_accuracy | inference_time_ms, model_size_mb |
| **모바일 앱** | build_ok, crash_free, startup_time_ms | memory_peak_mb, battery_impact |
| **게임** | build_ok, fps, physics_stable | draw_calls, load_time_s |
| **CAD/시뮬레이션** | build_ok, render_fps, physics_ok | node_count, solver_time_ms |

---

## 7. 철학

```
[과거] 인간 → 코드 → 컴퓨터
[현재] 인간 → 지시문(md) → 에이전트 → 코드 → 컴퓨터
[다음] 인간 → 목표 → 에이전트 → 지시문 → 에이전트 → 코드 → 컴퓨터
```

이 체계에서 인간의 역할은 세 가지다:

1. **GOAL.md를 쓴다** — 무엇을 만들지 정의한다
2. **program.md를 다듬는다** — 에이전트가 더 잘 판단하도록 지시를 개선한다
3. **DEVLOG.md를 읽는다** — 결과를 평가하고 방향을 수정한다

코드를 직접 짜는 것이 아니라,
코드를 짜는 에이전트에게 **어떻게 생각할지를 가르치는 글**을 쓰는 것이다.
